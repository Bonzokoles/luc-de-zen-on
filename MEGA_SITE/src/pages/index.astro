---
import Layout from '../layouts/Layout.astro';
import Tags from '../components/reactive/Tags.svelte';
import InteractiveQuizWidget from '../components/InteractiveQuizWidget.svelte';
---

<Layout 
  title="Home" 
  theme="cyberpunk"
  description="MEGA SITE - Zaawansowana platforma AI z Voice Assistant, Admin Dashboard, POLACZEK Agent System, Stable Diffusion i pełną obsługą języka polskiego"
>
  <!-- Hero Section -->
  <section class="relative min-h-screen flex items-center justify-center overflow-hidden">
    <!-- Animated background -->
    <div class="absolute inset-0 z-0">
      <div id="particles-container" class="w-full h-full"></div>
    </div>
    
    <div class="relative z-10 text-center max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="mb-8">
        <h1 class="glow text-6xl md:text-8xl font-neuropol text-cyan-400 mb-6 animate-pulse">
          MEGA SITE
        </h1>
        <div class="text-2xl md:text-4xl font-steelfish text-white mb-4">
          LUC de ZEN ON
        </div>
        <p class="text-xl md:text-2xl text-cyan-300 max-w-3xl mx-auto leading-relaxed">
          Zaawansowana platforma AI z pełną obsługą języka polskiego
        </p>
      </div>
      
      <!-- Feature Pills -->
      <div class="flex flex-wrap justify-center gap-4 mb-12">
        <span class="bg-glass px-4 py-2 rounded-full text-cyan-400 border border-cyan-500/30">Voice AI Assistant</span>
        <span class="bg-glass px-4 py-2 rounded-full text-cyan-400 border border-cyan-500/30">Admin Dashboard</span>
        <span class="bg-glass px-4 py-2 rounded-full text-cyan-400 border border-cyan-500/30">POLACZEK Agent</span>
        <span class="bg-glass px-4 py-2 rounded-full text-cyan-400 border border-cyan-500/30">Stable Diffusion</span>
        <span class="bg-glass px-4 py-2 rounded-full text-cyan-400 border border-cyan-500/30">Multi AI Chat</span>
      </div>
      
      <!-- CTA Buttons -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/dashboard" class="bg-cyan-600 hover:bg-cyan-700 text-white font-bold py-4 px-8 rounded-lg transition-all duration-300 transform hover:scale-105 glow">
          Rozpocznij Pracę
        </a>
        <a href="/admin" class="bg-glass border border-cyan-500/50 text-cyan-400 font-bold py-4 px-8 rounded-lg hover:bg-cyan-500/20 transition-all duration-300">
          Panel Administracyjny
        </a>
      </div>
    </div>
  </section>

  <!-- Features Grid -->
  <section class="py-20 bg-gray-900/50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-neuropol text-cyan-400 mb-4">Zaawansowane Funkcje AI</h2>
        <p class="text-xl text-gray-300 max-w-3xl mx-auto">
          Odkryj pełen potencjał sztucznej inteligencji z polskim interfejsem
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <!-- Voice AI Feature -->
        <div class="bg-glass rounded-xl p-8 border border-cyan-500/30 hover:border-cyan-500/60 transition-all duration-300 transform hover:scale-105">
          <div class="w-16 h-16 bg-gradient-to-br from-cyan-500 to-blue-600 rounded-lg mb-6 flex items-center justify-center">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11a7 7 0 01-7 7m0 0a7 7 0 01-7-7m7 7v4m0 0H8m4 0h4m-4-8a3 3 0 01-3-3V5a3 3 0 116 0v6a3 3 0 01-3 3z" />
            </svg>
          </div>
          <h3 class="text-2xl font-bold text-white mb-4">Voice AI Assistant</h3>
          <p class="text-gray-300 mb-6">Zaawansowany asystent głosowy z pełną obsługą języka polskiego, rozpoznawaniem mowy i syntezą głosu.</p>
          <a href="/voice-ai" class="text-cyan-400 hover:text-cyan-300 font-semibold">Wypróbuj →</a>
        </div>

        <!-- Admin Dashboard Feature -->
        <div class="bg-glass rounded-xl p-8 border border-cyan-500/30 hover:border-cyan-500/60 transition-all duration-300 transform hover:scale-105">
          <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg mb-6 flex items-center justify-center">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
            </svg>
          </div>
          <h3 class="text-2xl font-bold text-white mb-4">Admin Dashboard</h3>
          <p class="text-gray-300 mb-6">Kompletny panel administracyjny z monitoringiem systemu, analityką i zarządzaniem użytkownikami.</p>
          <a href="/admin" class="text-cyan-400 hover:text-cyan-300 font-semibold">Zarządzaj →</a>
        </div>

        <!-- POLACZEK Agent Feature -->
        <div class="bg-glass rounded-xl p-8 border border-cyan-500/30 hover:border-cyan-500/60 transition-all duration-300 transform hover:scale-105">
          <div class="w-16 h-16 bg-gradient-to-br from-red-500 to-pink-600 rounded-lg mb-6 flex items-center justify-center">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
            </svg>
          </div>
          <h3 class="text-2xl font-bold text-white mb-4">POLACZEK Agent</h3>
          <p class="text-gray-300 mb-6">Specjalny agent AI dostosowany do polskiej kultury i języka, z zaawansowanymi możliwościami konwersacyjnymi.</p>
          <a href="/polaczek" class="text-cyan-400 hover:text-cyan-300 font-semibold">Pogadaj →</a>
        </div>

        <!-- Stable Diffusion Feature -->
        <div class="bg-glass rounded-xl p-8 border border-cyan-500/30 hover:border-cyan-500/60 transition-all duration-300 transform hover:scale-105">
          <div class="w-16 h-16 bg-gradient-to-br from-purple-500 to-violet-600 rounded-lg mb-6 flex items-center justify-center">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
            </svg>
          </div>
          <h3 class="text-2xl font-bold text-white mb-4">Generowanie Obrazów</h3>
          <p class="text-gray-300 mb-6">Zaawansowany system generowania obrazów AI z Stable Diffusion, Photo2Photo i edytorem promptów.</p>
          <a href="/stable-diffusion" class="text-cyan-400 hover:text-cyan-300 font-semibold">Twórz →</a>
        </div>

        <!-- Multi AI Chat Feature -->
        <div class="bg-glass rounded-xl p-8 border border-cyan-500/30 hover:border-cyan-500/60 transition-all duration-300 transform hover:scale-105">
          <div class="w-16 h-16 bg-gradient-to-br from-green-500 to-emerald-600 rounded-lg mb-6 flex items-center justify-center">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8h2a2 2 0 012 2v6a2 2 0 01-2 2h-2v4l-4-4H9a2 2 0 01-2-2v-6a2 2 0 012-2h8z" />
            </svg>
          </div>
          <h3 class="text-2xl font-bold text-white mb-4">Multi AI Chat</h3>
          <p class="text-gray-300 mb-6">Czat z różnymi modelami AI: OpenAI, Bielik, DeepSeek, Gemma i więcej w jednym interfejsie.</p>
          <a href="/agents" class="text-cyan-400 hover:text-cyan-300 font-semibold">Rozmawiaj →</a>
        </div>

        <!-- Backroom System Feature -->
        <div class="bg-glass rounded-xl p-8 border border-cyan-500/30 hover:border-cyan-500/60 transition-all duration-300 transform hover:scale-105">
          <div class="w-16 h-16 bg-gradient-to-br from-orange-500 to-red-600 rounded-lg mb-6 flex items-center justify-center">
            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
            </svg>
          </div>
          <h3 class="text-2xl font-bold text-white mb-4">Backroom System</h3>
          <p class="text-gray-300 mb-6">Zaawansowany system zarządzania backendem, worker management i API orchestration.</p>
          <a href="/backroom" class="text-cyan-400 hover:text-cyan-300 font-semibold">Kontroluj →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Interactive Demo Section -->
  <section class="py-20 bg-gradient-to-br from-gray-800 via-gray-900 to-black">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-neuropol text-cyan-400 mb-4">Interaktywne Demonstracje</h2>
        <p class="text-xl text-gray-300 max-w-3xl mx-auto">
          Wypróbuj nasze funkcje bezpośrednio w przeglądarce
        </p>
      </div>
      
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
        <!-- Tags Demo -->
        <div class="bg-glass rounded-xl p-8 border border-cyan-500/30">
          <h3 class="text-2xl font-bold text-white mb-6">Interaktywne Tagi</h3>
          <p class="text-gray-300 mb-6">System tagów z możliwością dodawania, usuwania i filtrowania w czasie rzeczywistym.</p>
          <div class="bg-gray-800/50 rounded-lg p-6">
            <Tags client:load />
          </div>
        </div>

        <!-- Quiz Demo -->
        <div class="bg-glass rounded-xl p-8 border border-cyan-500/30">
          <h3 class="text-2xl font-bold text-white mb-6">Quiz AI</h3>
          <p class="text-gray-300 mb-6">Interaktywny quiz generowany przez AI z polskimi pytaniami i odpowiedziami.</p>
          <div class="bg-gray-800/50 rounded-lg p-6">
            <InteractiveQuizWidget client:load />
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Statistics Section -->
  <section class="py-20 bg-gray-900/50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
        <div class="text-center">
          <div class="text-4xl font-neuropol text-cyan-400 mb-2" data-counter="10000">0</div>
          <div class="text-gray-300">Użytkowników</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-neuropol text-cyan-400 mb-2" data-counter="50000">0</div>
          <div class="text-gray-300">Zapytań AI</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-neuropol text-cyan-400 mb-2" data-counter="25000">0</div>
          <div class="text-gray-300">Wygenerowanych Obrazów</div>
        </div>
        <div class="text-center">
          <div class="text-4xl font-neuropol text-cyan-400 mb-2" data-counter="99">0</div>
          <div class="text-gray-300">% Uptime</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Polish Language Showcase -->
  <section class="py-20 bg-gradient-to-r from-red-900/20 via-gray-900 to-red-900/20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <div class="mb-12">
        <h2 class="text-4xl font-neuropol text-white mb-4">🇵🇱 Pełna Obsługa Języka Polskiego</h2>
        <p class="text-xl text-gray-300 max-w-4xl mx-auto leading-relaxed">
          Pierwsza zaawansowana platforma AI z native obsługą języka polskiego. 
          Wszystkie funkcje zostały dostosowane do polskiej gramatyki, kultury i specyfiki językowej.
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-12">
        <div class="bg-glass rounded-lg p-6 border border-red-500/30">
          <h4 class="text-lg font-bold text-red-400 mb-3">Gramatyka Polska</h4>
          <p class="text-gray-300 text-sm">Pełne wsparcie dla odmiany, koniugacji i składni języka polskiego</p>
        </div>
        <div class="bg-glass rounded-lg p-6 border border-red-500/30">
          <h4 class="text-lg font-bold text-red-400 mb-3">Kultura i Kontekst</h4>
          <p class="text-gray-300 text-sm">AI rozumie polski kontekst kulturowy, historyczny i społeczny</p>
        </div>
        <div class="bg-glass rounded-lg p-6 border border-red-500/30">
          <h4 class="text-lg font-bold text-red-400 mb-3">Lokalne Zasoby</h4>
          <p class="text-gray-300 text-sm">Bazy danych dostosowane do polskich realiów i potrzeb</p>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 bg-gradient-to-br from-cyan-900/30 via-gray-900 to-purple-900/30">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
      <h2 class="text-4xl font-neuropol text-cyan-400 mb-6">Gotowy na Przyszłość AI?</h2>
      <p class="text-xl text-gray-300 mb-10 leading-relaxed">
        Dołącz do tysięcy użytkowników, którzy już korzystają z zaawansowanych funkcji MEGA SITE. 
        Rozpocznij swoją przygodę ze sztuczną inteligencją już dziś.
      </p>
      
      <div class="flex flex-col sm:flex-row gap-6 justify-center">
        <a href="/dashboard" class="bg-gradient-to-r from-cyan-600 to-blue-600 hover:from-cyan-700 hover:to-blue-700 text-white font-bold py-4 px-8 rounded-lg transition-all duration-300 transform hover:scale-105 glow text-lg">
          🚀 Rozpocznij Teraz
        </a>
        <a href="/docs" class="bg-glass border border-cyan-500/50 text-cyan-400 font-bold py-4 px-8 rounded-lg hover:bg-cyan-500/20 transition-all duration-300 text-lg">
          📚 Dokumentacja
        </a>
      </div>
    </div>
  </section>
</Layout>

<script>
  // Particles Animation
  function createParticles() {
    const container = document.getElementById('particles-container');
    if (!container) return;

    for (let i = 0; i < 100; i++) {
      const particle = document.createElement('div');
      particle.className = 'absolute w-1 h-1 bg-cyan-400 rounded-full opacity-70';
      particle.style.left = Math.random() * 100 + '%';
      particle.style.top = Math.random() * 100 + '%';
      particle.style.animationDuration = (Math.random() * 3 + 2) + 's';
      particle.style.animationDelay = Math.random() * 2 + 's';
      
      const keyframes = `
        @keyframes float-${i} {
          0%, 100% { transform: translateY(0px) translateX(0px); opacity: 0.7; }
          33% { transform: translateY(-${Math.random() * 20 + 10}px) translateX(${Math.random() * 20 - 10}px); opacity: 1; }
          66% { transform: translateY(${Math.random() * 20 + 10}px) translateX(${Math.random() * 20 - 10}px); opacity: 0.5; }
        }
      `;
      
      if (!document.getElementById(`style-${i}`)) {
        const style = document.createElement('style');
        style.id = `style-${i}`;
        style.textContent = keyframes;
        document.head.appendChild(style);
      }
      
      particle.style.animation = `float-${i} ${Math.random() * 3 + 2}s ease-in-out infinite`;
      container.appendChild(particle);
    }
  }

  // Counter Animation
  function animateCounters() {
    const counters = document.querySelectorAll('[data-counter]');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const counter = entry.target;
          const target = parseInt(counter.getAttribute('data-counter'));
          const duration = 2000; // 2 seconds
          const step = target / (duration / 16); // 60 FPS
          let current = 0;

          const timer = setInterval(() => {
            current += step;
            if (current >= target) {
              current = target;
              clearInterval(timer);
            }
            counter.textContent = Math.floor(current).toLocaleString('pl-PL');
          }, 16);

          observer.unobserve(counter);
        }
      });
    });

    counters.forEach(counter => observer.observe(counter));
  }

  // Feature Cards Hover Effect
  function addHoverEffects() {
    const cards = document.querySelectorAll('.bg-glass');
    cards.forEach(card => {
      card.addEventListener('mouseenter', () => {
        card.style.boxShadow = '0 0 30px rgba(34, 211, 238, 0.3)';
      });
      card.addEventListener('mouseleave', () => {
        card.style.boxShadow = 'none';
      });
    });
  }

  // Initialize everything when DOM is loaded
  document.addEventListener('DOMContentLoaded', () => {
    createParticles();
    animateCounters();
    addHoverEffects();
  });

  // Performance monitoring
  if ('performance' in window) {
    window.addEventListener('load', () => {
      const loadTime = performance.timing.loadEventEnd - performance.timing.navigationStart;
      console.log(`🚀 MEGA SITE loaded in ${loadTime}ms`);
      
      // Send performance data to analytics
      if (loadTime > 3000) {
        console.warn('⚠️ Page load time is slow. Consider optimization.');
      }
    });
  }
</script>

<style>
  /* Enhanced animations */
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
  
  @keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }
  
  /* Gradient text effects */
  .text-gradient {
    background: linear-gradient(45deg, #06b6d4, #3b82f6, #8b5cf6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  
  /* Interactive elements */
  .interactive-card {
    transition: all 0.3s ease;
  }
  
  .interactive-card:hover {
    transform: translateY(-5px) scale(1.02);
    box-shadow: 0 20px 40px rgba(34, 211, 238, 0.2);
  }
  
  /* Polish flag colors */
  .poland-gradient {
    background: linear-gradient(180deg, #dc2626 0%, #dc2626 50%, #ffffff 50%, #ffffff 100%);
  }
  
  /* Responsive improvements */
  @media (max-width: 768px) {
    .glow {
      text-shadow: 0 0 10px currentColor;
    }
    
    .text-6xl {
      font-size: 3rem;
    }
    
    .text-8xl {
      font-size: 4rem;
    }
  }
</style>
