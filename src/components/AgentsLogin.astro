---
// Agents Login Component - Separate Authentication System
---

<!-- Agents Login Container -->
<div id="agents-login-container" class="agents-login-wrapper">
  <div class="agents-login-content">
    <div class="agents-login-header">
      <div class="agents-logo">
        <svg
          width="40"
          height="40"
          viewBox="0 0 24 24"
          fill="none"
          xmlns="http://www.w3.org/2000/svg"
        >
          <path
            d="M12 2L2 7L12 12L22 7L12 2Z"
            stroke="currentColor"
            stroke-width="2"
            stroke-linejoin="round"></path>
          <path
            d="M2 17L12 22L22 17"
            stroke="currentColor"
            stroke-width="2"
            stroke-linejoin="round"></path>
          <path
            d="M2 12L12 17L22 12"
            stroke="currentColor"
            stroke-width="2"
            stroke-linejoin="round"></path>
        </svg>
        <h2>SYSTEM AGENTÓW</h2>
      </div>
      <p class="agents-subtitle">Dostęp do zaawansowanego systemu AI Agents</p>
    </div>

    <form
      id="agents-login-form"
      onsubmit="handleAgentsLogin(event)"
      class="agents-form"
    >
      <div class="agents-form-group">
        <label for="agents-password">Kod dostępu do systemu agentów:</label>
        <input
          type="password"
          id="agents-password"
          placeholder="Wprowadź kod BONZO23"
          required
          autocomplete="off"
        />
      </div>

      <div id="agents-error" class="agents-error hidden"></div>
      <div id="agents-success" class="agents-success hidden"></div>

      <button type="submit" class="agents-submit-btn" id="agents-submit-btn">
        <span class="btn-text">Uzyskaj dostęp do systemu</span>
        <div class="btn-loader hidden">
          <div class="spinner"></div>
          <span>Uwierzytelnianie...</span>
        </div>
      </button>
    </form>

    <div class="agents-info">
      <div class="info-item">
        <strong>🤖 System Agentów AI</strong>
        <p>Tworzenie, zarządzanie i monitoring inteligentnych agentów</p>
      </div>
      <div class="info-item">
        <strong>🛠️ Advanced Builder</strong>
        <p>Wizualny kreator agentów z interfejsem drag-and-drop</p>
      </div>
      <div class="info-item">
        <strong>📊 Analytics Dashboard</strong>
        <p>Pełne statystyki i monitoring wydajności agentów</p>
      </div>
    </div>
  </div>
</div>

<style>
  .agents-login-wrapper {
    background: rgba(15, 15, 15, 0.95);
    border: 2px solid rgba(0, 217, 255, 0.4);
    border-radius: 16px;
    padding: 0;
    max-width: 500px;
    width: 100%;
    margin: 40px auto;
    box-shadow: 0 0 40px rgba(0, 217, 255, 0.2);
    backdrop-filter: blur(10px);
    overflow: hidden;
  }

  .agents-login-content {
    padding: 0;
  }

  .agents-login-header {
    background: linear-gradient(
      135deg,
      rgba(0, 217, 255, 0.1),
      rgba(0, 150, 200, 0.05)
    );
    padding: 30px;
    text-align: center;
    border-bottom: 1px solid rgba(0, 217, 255, 0.3);
  }

  .agents-logo {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 15px;
    margin-bottom: 15px;
  }

  .agents-logo svg {
    color: #00d9ff;
    filter: drop-shadow(0 0 10px rgba(0, 217, 255, 0.5));
  }

  .agents-logo h2 {
    color: #00d9ff;
    margin: 0;
    font-size: 24px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 2px;
    text-shadow: 0 0 10px rgba(0, 217, 255, 0.3);
  }

  .agents-subtitle {
    color: #b0b0b0;
    margin: 0;
    font-size: 14px;
    opacity: 0.9;
  }

  .agents-form {
    padding: 30px;
  }

  .agents-form-group {
    margin-bottom: 25px;
  }

  .agents-form-group label {
    display: block;
    color: #00d9ff;
    margin-bottom: 10px;
    font-weight: 600;
    font-size: 14px;
  }

  .agents-form-group input {
    width: 100%;
    padding: 15px;
    background: rgba(30, 30, 30, 0.8);
    border: 2px solid rgba(0, 217, 255, 0.3);
    border-radius: 8px;
    color: #ffffff;
    font-size: 16px;
    font-family: "Courier New", monospace;
    letter-spacing: 2px;
    text-align: center;
    transition: all 0.3s ease;
  }

  .agents-form-group input:focus {
    outline: none;
    border-color: rgba(0, 217, 255, 0.6);
    box-shadow: 0 0 15px rgba(0, 217, 255, 0.2);
    background: rgba(40, 40, 40, 0.9);
  }

  .agents-form-group input::placeholder {
    color: #666;
    letter-spacing: 1px;
  }

  .agents-submit-btn {
    width: 100%;
    padding: 15px;
    background: linear-gradient(135deg, #00d9ff, #0099cc);
    border: none;
    border-radius: 8px;
    color: #000;
    font-size: 16px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .agents-submit-btn:hover {
    background: linear-gradient(135deg, #00ccff, #0088bb);
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0, 217, 255, 0.3);
  }

  .agents-submit-btn:active {
    transform: translateY(0);
  }

  .btn-loader {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
  }

  .spinner {
    width: 16px;
    height: 16px;
    border: 2px solid rgba(0, 0, 0, 0.3);
    border-top: 2px solid #000;
    border-radius: 50%;
    animation: spin 1s linear infinite;
  }

  @keyframes spin {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }

  .agents-error {
    background: rgba(255, 50, 50, 0.1);
    border: 1px solid rgba(255, 50, 50, 0.3);
    color: #ff6666;
    padding: 15px;
    border-radius: 8px;
    margin-bottom: 20px;
    font-size: 14px;
    text-align: center;
  }

  .agents-success {
    background: rgba(50, 255, 50, 0.1);
    border: 1px solid rgba(50, 255, 50, 0.3);
    color: #66ff66;
    padding: 15px;
    border-radius: 8px;
    margin-bottom: 20px;
    font-size: 14px;
    text-align: center;
  }

  .agents-info {
    background: rgba(0, 217, 255, 0.05);
    padding: 25px;
    border-top: 1px solid rgba(0, 217, 255, 0.2);
  }

  .info-item {
    margin-bottom: 20px;
    padding-left: 10px;
    border-left: 3px solid rgba(0, 217, 255, 0.4);
  }

  .info-item:last-child {
    margin-bottom: 0;
  }

  .info-item strong {
    color: #00d9ff;
    display: block;
    margin-bottom: 5px;
    font-size: 14px;
  }

  .info-item p {
    color: #b0b0b0;
    margin: 0;
    font-size: 13px;
    line-height: 1.4;
  }

  .hidden {
    display: none !important;
  }
</style>

<script>
  // Agents Login Functions
  function handleAgentsLogin(event) {
    event.preventDefault();

    const password = document.getElementById("agents-password").value;
    const errorDiv = document.getElementById("agents-error");
    const successDiv = document.getElementById("agents-success");
    const submitBtn = document.getElementById("agents-submit-btn");
    const btnText = submitBtn.querySelector(".btn-text");
    const btnLoader = submitBtn.querySelector(".btn-loader");

    // Reset messages
    errorDiv.classList.add("hidden");
    successDiv.classList.add("hidden");

    // Show loading state
    btnText.classList.add("hidden");
    btnLoader.classList.remove("hidden");
    submitBtn.disabled = true;

    // Simulate authentication delay
    setTimeout(() => {
      if (password === "BONZO23") {
        // Zapisz dane uwierzytelniania agentów
        const agentsAuthData = {
          authenticated: true,
          userType: "agents-user",
          system: "agents-system",
          loginTime: new Date().toISOString(),
          accessLevel: "full",
        };

        sessionStorage.setItem(
          "mybonzo_agents_auth",
          JSON.stringify(agentsAuthData)
        );

        // Pokaż sukces
        successDiv.textContent =
          "✅ Uwierzytelnienie pomyślne! Przekierowywanie do systemu agentów...";
        successDiv.classList.remove("hidden");

        console.log("🤖 Dostęp do systemu agentów:", agentsAuthData);

        // Przekieruj z opóźnieniem
        setTimeout(() => {
          // Ukryj kontener logowania i pokaż interfejs agentów
          document.getElementById("agents-login-container").style.display =
            "none";

          // Uruchom system agentów
          initializeAgentsSystem();
        }, 1500);
      } else {
        errorDiv.textContent =
          "❌ Nieprawidłowy kod dostępu do systemu agentów!";
        errorDiv.classList.remove("hidden");
        document.getElementById("agents-password").value = "";
        document.getElementById("agents-password").focus();
      }

      // Reset button state
      btnText.classList.remove("hidden");
      btnLoader.classList.add("hidden");
      submitBtn.disabled = false;
    }, 2000); // 2 second authentication simulation
  }

  function initializeAgentsSystem() {
    // Pokaż że system agentów jest gotowy
    const container = document.getElementById("agents-login-container");
    if (container) {
      container.innerHTML = `
        <div class="agents-ready-state">
          <div class="ready-header">
            <div class="status-indicator">
              <div class="pulse-dot"></div>
              <span>SYSTEM AGENTÓW AKTYWNY</span>
            </div>
          </div>
          <div class="ready-content">
            <h3>🚀 System gotowy do użycia</h3>
            <p>Uwierzytelnienie zakończone pomyślnie. System agentów AI jest teraz dostępny.</p>
            <div class="ready-actions">
              <button onclick="window.location.reload()" class="refresh-btn">
                Odśwież interfejs
              </button>
            </div>
          </div>
        </div>
      `;

      // Dodaj style dla ready state
      const style = document.createElement("style");
      style.textContent = `
        .agents-ready-state {
          text-align: center;
          padding: 40px;
          background: rgba(0, 217, 255, 0.1);
          border-radius: 16px;
        }
        .status-indicator {
          display: flex;
          align-items: center;
          justify-content: center;
          gap: 10px;
          margin-bottom: 30px;
        }
        .pulse-dot {
          width: 12px;
          height: 12px;
          background: #00ff00;
          border-radius: 50%;
          animation: pulse 2s infinite;
        }
        @keyframes pulse {
          0%, 100% { opacity: 1; transform: scale(1); }
          50% { opacity: 0.5; transform: scale(1.2); }
        }
        .status-indicator span {
          color: #00d9ff;
          font-weight: 700;
          text-transform: uppercase;
          letter-spacing: 1px;
        }
        .ready-content h3 {
          color: #00d9ff;
          margin-bottom: 15px;
        }
        .ready-content p {
          color: #b0b0b0;
          margin-bottom: 30px;
        }
        .refresh-btn {
          background: linear-gradient(135deg, #00d9ff, #0099cc);
          border: none;
          border-radius: 8px;
          color: #000;
          padding: 12px 24px;
          font-weight: 600;
          cursor: pointer;
          transition: all 0.3s ease;
        }
        .refresh-btn:hover {
          transform: translateY(-2px);
          box-shadow: 0 5px 15px rgba(0, 217, 255, 0.3);
        }
      `;
      document.head.appendChild(style);
    }
  }

  // Sprawdź czy użytkownik jest już zalogowany do systemu agentów
  document.addEventListener("DOMContentLoaded", function () {
    const agentsAuth = sessionStorage.getItem("mybonzo_agents_auth");
    if (agentsAuth) {
      try {
        const parsed = JSON.parse(agentsAuth);
        if (parsed.authenticated && parsed.system === "agents-system") {
          console.log("🤖 Użytkownik już zalogowany do systemu agentów");
          initializeAgentsSystem();
        }
      } catch (e) {
        console.log("Błąd parsowania danych uwierzytelniania agentów");
      }
    }
  });
</script>
