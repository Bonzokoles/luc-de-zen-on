---
// Komponent demonstracyjny polskich fontów w admin panelu
---

<div class="polish-fonts-demo" style="margin: 20px 0; padding: 20px; background: rgba(0,0,0,0.3); border: 1px solid rgba(0,217,255,0.3); border-radius: 12px;">
    <h3 style="color: #00d9ff; margin-bottom: 20px; text-align: center;">
        🎨 Demo Polskich Fontów w Admin Panelu
    </h3>
    
    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 15px;">
        
        <!-- Neuropol Demo -->
        <div style="background: rgba(220,20,60,0.1); padding: 15px; border-radius: 8px; border: 1px solid rgba(220,20,60,0.3);">
            <h4 style="font-family: 'Neuropol', system-ui, sans-serif; color: #dc143c; margin-bottom: 10px; font-size: 1.3rem; letter-spacing: 0.05em;">
                NEUROPOL FONT
            </h4>
            <p style="font-family: 'Neuropol', system-ui, sans-serif; color: rgba(255,255,255,0.8); font-size: 0.9rem;">
                Panel Administracyjny MyBonzo<br>
                Zarządzanie systemem AI<br>
                Możliwości bez granic<br>
                Łączymy przyszłość z teraźniejszością
            </p>
        </div>

        <!-- Kenyan Coffee Demo -->
        <div style="background: rgba(0,217,255,0.1); padding: 15px; border-radius: 8px; border: 1px solid rgba(0,217,255,0.3);">
            <h4 style="font-family: 'Kenyan Coffee', system-ui, sans-serif; color: #00d9ff; margin-bottom: 10px; font-size: 1.3rem; letter-spacing: 0.03em;">
                Kenyan Coffee Font
            </h4>
            <p style="font-family: 'Kenyan Coffee', system-ui, sans-serif; color: rgba(255,255,255,0.8); font-size: 0.9rem;">
                Witaj w panelu administracyjnym!<br>
                Twój asystent AI jest gotowy<br>
                Śmiało eksploruj wszystkie funkcje<br>
                Żywotność systemu: 99.8%
            </p>
        </div>

        <!-- Rajdhani Demo -->
        <div style="background: rgba(139,92,246,0.1); padding: 15px; border-radius: 8px; border: 1px solid rgba(139,92,246,0.3);">
            <h4 style="font-family: 'Rajdhani', system-ui, sans-serif; color: #8b5cf6; margin-bottom: 10px; font-size: 1.3rem; font-weight: 600; letter-spacing: 0.04em;">
                Rajdhani Font
            </h4>
            <p style="font-family: 'Rajdhani', system-ui, sans-serif; color: rgba(255,255,255,0.8); font-size: 0.9rem; font-weight: 400;">
                Statystyki działają właściwie<br>
                Użytkownicy: Aktywni i zaangażowani<br>
                Błędy: Minimalne występowanie<br>
                Prędkość odpowiedzi: Błyskawiczna
            </p>
        </div>
    </div>

    <!-- Przyciski testowe -->
    <div style="text-align: center; margin-top: 20px;">
        <button 
            onclick="document.querySelector('.polish-fonts-demo').style.transform = 'scale(1.05)'; setTimeout(() => document.querySelector('.polish-fonts-demo').style.transform = 'scale(1)', 200)"
            style="
                background: linear-gradient(135deg, #dc143c 0%, #8b0000 50%, #dc143c 100%);
                border: 2px solid #dc143c;
                color: white;
                padding: 10px 20px;
                border-radius: 8px;
                font-family: 'Neuropol', system-ui, sans-serif;
                letter-spacing: 0.05em;
                cursor: pointer;
                margin: 0 5px;
                transition: all 0.3s ease;
            "
            onmouseover="this.style.boxShadow='0 0 20px rgba(220,20,60,0.6)'"
            onmouseout="this.style.boxShadow='none'"
        >
            🇵🇱 TEST POLSKICH FONTÓW
        </button>
        
        <button 
            onclick="refreshAdminFonts(); alert('Polskie fonty zostały odświeżone! 🎨')"
            style="
                background: linear-gradient(135deg, #00d9ff 0%, #0066cc 50%, #00d9ff 100%);
                border: 2px solid #00d9ff;
                color: white;
                padding: 10px 20px;
                border-radius: 8px;
                font-family: 'Kenyan Coffee', system-ui, sans-serif;
                letter-spacing: 0.03em;
                cursor: pointer;
                margin: 0 5px;
                transition: all 0.3s ease;
            "
            onmouseover="this.style.boxShadow='0 0 20px rgba(0,217,255,0.6)'"
            onmouseout="this.style.boxShadow='none'"
        >
            🔄 LOSUJ PONOWNIE
        </button>
    </div>

    <!-- Info o aktualnych fontach -->
    <div id="current-fonts-info" style="margin-top: 15px; padding: 10px; background: rgba(0,0,0,0.5); border-radius: 6px; font-size: 0.85rem; color: rgba(255,255,255,0.6); text-align: center;">
        <strong>Aktualne polskie fonty:</strong> Neuropol • Kenyan Coffee • Rajdhani
    </div>
</div>

<style>
    .polish-fonts-demo {
        transition: transform 0.2s ease;
    }
    
    .polish-fonts-demo:hover {
        border-color: rgba(0,217,255,0.5);
        box-shadow: 0 5px 15px rgba(0,217,255,0.2);
    }
</style>

<script is:inline>
    // Dodatkowy skrypt do demo polskich fontów
    document.addEventListener('DOMContentLoaded', function() {
        // Aktualizuj info o fontach co 3 sekundy
        setInterval(function() {
            const info = document.getElementById('current-fonts-info');
            if (info && window.location.pathname.includes('/admin')) {
                const root = document.documentElement;
                const headingFont = root.style.getPropertyValue('--admin-heading-font') || 'Neuropol';
                const textFont = root.style.getPropertyValue('--admin-text-font') || 'Kenyan Coffee';
                const buttonFont = root.style.getPropertyValue('--admin-button-font') || 'Rajdhani';
                
                info.innerHTML = `<strong>Aktualne polskie fonty:</strong> ${headingFont.replace(/"/g, '')} • ${textFont.replace(/"/g, '')} • ${buttonFont.replace(/"/g, '')}`;
            }
        }, 3000);
    });
</script>