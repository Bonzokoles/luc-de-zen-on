---
import Layout from '../layouts/Layout.astro';
import BgAnimation from '../components/BgAnimation.svelte';
---

<Layout siteTitle="Funkcje Biznesowe AI - Luc de Zen'On" siteDescription="Zaawansowane narzędzia biznesowe oparte na sztucznej inteligencji" headerCosmeticText="FUNKCJE BIZNESOWE AI">
  <main class="min-h-screen bg-black text-cyan-300 relative overflow-hidden">
    <!-- Background Animation -->
    <div class="absolute inset-0 z-0">
      <BgAnimation client:load />
    </div>
    
    <!-- Overlay -->
    <div class="absolute inset-0 bg-gradient-to-br from-black/90 via-gray-900/80 to-black/90 z-10"></div>
    
    <!-- Content -->
    <div class="relative z-20">
      <!-- Header -->
      <div class="pt-8 pb-12 px-6">
        <div class="max-w-7xl mx-auto text-center">
          <div class="flex items-center justify-center mb-6">
            <div class="w-4 h-4 bg-cyan-400 rounded-full animate-pulse mr-4"></div>
            <h1 class="text-4xl md:text-6xl font-bold bg-gradient-to-r from-cyan-400 via-blue-500 to-purple-600 bg-clip-text text-transparent">
              FUNKCJE BIZNESOWE AI
            </h1>
            <div class="w-4 h-4 bg-purple-400 rounded-full animate-pulse ml-4"></div>
          </div>
          
          <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-8">
            <div class="bg-black/50 backdrop-blur-sm border border-cyan-500/30 rounded-xl p-6">
              <div class="text-cyan-400 text-2xl mb-2">🔍</div>
              <h3 class="text-xl font-semibold mb-2 text-cyan-400">MONITOROWANIE</h3>
              <p class="text-sm text-gray-300">Śledź aktywność systemu, wykrywaj anomalie i generuj raporty w czasie rzeczywistym</p>
            </div>
            <div class="bg-black/50 backdrop-blur-sm border border-purple-500/30 rounded-xl p-6">
              <div class="text-purple-400 text-2xl mb-2">📅</div>
              <h3 class="text-xl font-semibold mb-2 text-purple-400">HARMONOGRAM</h3>
              <p class="text-sm text-gray-300">Zarządzaj przypomnieniami, powiadomieniami i integracją z kalendarzami</p>
            </div>
            <div class="bg-black/50 backdrop-blur-sm border border-yellow-500/30 rounded-xl p-6">
              <div class="text-yellow-400 text-2xl mb-2">❓</div>
              <h3 class="text-xl font-semibold mb-2 text-yellow-400">FAQ DYNAMICZNE</h3>
              <p class="text-sm text-gray-300">Generuj automatycznie FAQ na podstawie bazy wiedzy za pomocą AI</p>
            </div>
          </div>
          
          <p class="text-xl text-gray-400 max-w-4xl mx-auto leading-relaxed">
            Zaawansowane narzędzia biznesowe oparte na sztucznej inteligencji. 
            <span class="text-cyan-400 font-semibold">Monitoruj</span>, 
            <span class="text-purple-400 font-semibold">planuj</span> i 
            <span class="text-yellow-400 font-semibold">automatyzuj</span> 
            procesy w swojej organizacji.
          </p>
        </div>
      </div>

      <!-- Navigation Cards -->
      <div class="px-6 mb-12">
        <div class="max-w-7xl mx-auto">
          <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
            <a href="#monitoring" 
               class="group bg-black/40 backdrop-blur-sm border border-cyan-500/30 rounded-xl p-6 hover:border-cyan-400/60 hover:bg-cyan-900/10 transition-all duration-300 transform hover:scale-105">
              <div class="flex items-center mb-4">
                <div class="w-3 h-3 bg-cyan-400 rounded-full animate-pulse mr-3"></div>
                <h3 class="text-xl font-bold text-cyan-400 group-hover:text-cyan-300">Monitor Aktywności</h3>
              </div>
              <p class="text-gray-400 text-sm mb-4">Zbieraj logi, wykrywaj anomalie i generuj raporty o aktywności systemu</p>
              <div class="flex items-center text-cyan-400 group-hover:text-cyan-300">
                <span class="text-sm font-semibold">Uruchom moduł</span>
                <svg class="w-4 h-4 ml-2 transform group-hover:translate-x-1 transition-transform" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd"></path>
                </svg>
              </div>
            </a>

            <a href="#reminders" 
               class="group bg-black/40 backdrop-blur-sm border border-purple-500/30 rounded-xl p-6 hover:border-purple-400/60 hover:bg-purple-900/10 transition-all duration-300 transform hover:scale-105">
              <div class="flex items-center mb-4">
                <div class="w-3 h-3 bg-purple-400 rounded-full animate-pulse mr-3"></div>
                <h3 class="text-xl font-bold text-purple-400 group-hover:text-purple-300">Harmonogram AI</h3>
              </div>
              <p class="text-gray-400 text-sm mb-4">Twórz przypomnienia, synchronizuj kalendarze i zarządzaj powiadomieniami</p>
              <div class="flex items-center text-purple-400 group-hover:text-purple-300">
                <span class="text-sm font-semibold">Uruchom moduł</span>
                <svg class="w-4 h-4 ml-2 transform group-hover:translate-x-1 transition-transform" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd"></path>
                </svg>
              </div>
            </a>

            <a href="#faq" 
               class="group bg-black/40 backdrop-blur-sm border border-yellow-500/30 rounded-xl p-6 hover:border-yellow-400/60 hover:bg-yellow-900/10 transition-all duration-300 transform hover:scale-105">
              <div class="flex items-center mb-4">
                <div class="w-3 h-3 bg-yellow-400 rounded-full animate-pulse mr-3"></div>
                <h3 class="text-xl font-bold text-yellow-400 group-hover:text-yellow-300">Generator FAQ</h3>
              </div>
              <p class="text-gray-400 text-sm mb-4">Automatycznie generuj FAQ na podstawie dokumentacji i bazy wiedzy</p>
              <div class="flex items-center text-yellow-400 group-hover:text-yellow-300">
                <span class="text-sm font-semibold">Uruchom moduł</span>
                <svg class="w-4 h-4 ml-2 transform group-hover:translate-x-1 transition-transform" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd"></path>
                </svg>
              </div>
            </a>
          </div>
        </div>
      </div>

      <!-- Function Sections -->
      <div class="px-6 pb-12 space-y-16">
        <div class="max-w-7xl mx-auto">
          
          <!-- Activity Monitoring Section -->
          <section id="monitoring" class="scroll-mt-8">
            <div class="mb-8 text-center">
              <h2 class="text-3xl font-bold text-cyan-400 mb-2">🔍 MONITOR AKTYWNOŚCI SYSTEMU</h2>
              <p class="text-gray-400">Śledzenie aktywności w czasie rzeczywistym z automatyczną detekcją anomalii</p>
            </div>
            
            <div id="activity-dashboard-container"></div>
          </section>

          <!-- Reminders Section -->
          <section id="reminders" class="scroll-mt-8">
            <div class="mb-8 text-center">
              <h2 class="text-3xl font-bold text-purple-400 mb-2">📅 SYSTEM HARMONOGRAMOWANIA</h2>
              <p class="text-gray-400">Zarządzanie przypomnieniami z integracją AI i powiadomieniami</p>
            </div>
            
            <div id="reminders-manager-container"></div>
          </section>

          <!-- FAQ Generator Section -->
          <section id="faq" class="scroll-mt-8">
            <div class="mb-8 text-center">
              <h2 class="text-3xl font-bold text-yellow-400 mb-2">❓ GENERATOR FAQ DYNAMICZNY</h2>
              <p class="text-gray-400">Automatyczne tworzenie FAQ na podstawie bazy wiedzy z użyciem AI</p>
            </div>
            
            <div id="faq-generator-container"></div>
          </section>

        </div>
      </div>

      <!-- Status Bar -->
      <div class="fixed bottom-0 left-0 right-0 bg-black/90 border-t border-cyan-500/30 backdrop-blur-sm z-30">
        <div class="max-w-7xl mx-auto px-6 py-3">
          <div class="flex items-center justify-between">
            <div class="flex items-center space-x-6">
              <div class="flex items-center space-x-2">
                <div class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></div>
                <span class="text-xs text-green-400 font-mono">SYSTEM ONLINE</span>
              </div>
              <div class="flex items-center space-x-2">
                <div class="w-2 h-2 bg-cyan-400 rounded-full"></div>
                <span class="text-xs text-cyan-400 font-mono">AI MODULES ACTIVE</span>
              </div>
              <div class="flex items-center space-x-2">
                <div class="w-2 h-2 bg-purple-400 rounded-full"></div>
                <span class="text-xs text-purple-400 font-mono">MONITORING ENABLED</span>
              </div>
            </div>
            <div class="text-xs text-gray-400 font-mono">
              <span id="current-time"></span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
</Layout>

<script>
  import ActivityDashboard from '../components/ActivityDashboard.svelte';
  import RemindersManager from '../components/RemindersManager.svelte';
  import FAQGeneratorWidget from '../components/FAQGeneratorWidget.svelte';
  import { mount } from 'svelte';

  // Initialize components
  document.addEventListener('DOMContentLoaded', () => {
    // Activity Dashboard
    const activityContainer = document.getElementById('activity-dashboard-container');
    if (activityContainer) {
      mount(ActivityDashboard, {
        target: activityContainer
      });
    }

    // Reminders Manager
    const remindersContainer = document.getElementById('reminders-manager-container');
    if (remindersContainer) {
      mount(RemindersManager, {
        target: remindersContainer
      });
    }

    // FAQ Generator
    const faqContainer = document.getElementById('faq-generator-container');
    if (faqContainer) {
      mount(FAQGeneratorWidget, {
        target: faqContainer
      });
    }

    // Update time every second
    function updateTime() {
      const now = new Date();
      const timeString = now.toLocaleString('pl-PL', {
        day: '2-digit',
        month: '2-digit',
        year: 'numeric',
        hour: '2-digit',
        minute: '2-digit',
        second: '2-digit'
      });
      const timeElement = document.getElementById('current-time');
      if (timeElement) {
        timeElement.textContent = `CZAS LOKALNY: ${timeString}`;
      }
    }

    updateTime();
    setInterval(updateTime, 1000);

    // Smooth scrolling for navigation
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });

    // Add parallax effect to background elements
    let ticking = false;
    
    function updateParallax() {
      const scrolled = window.pageYOffset;
      const parallaxElements = document.querySelectorAll('.parallax-bg');
      
      parallaxElements.forEach(element => {
        const speed = element.dataset.speed || 0.5;
        const yPos = -(scrolled * speed);
        element.style.transform = `translate3d(0, ${yPos}px, 0)`;
      });
      
      ticking = false;
    }
    
    function requestTick() {
      if (!ticking) {
        requestAnimationFrame(updateParallax);
        ticking = true;
      }
    }
    
    window.addEventListener('scroll', requestTick);

    // Add glow effect on hover for interactive elements
    const interactiveElements = document.querySelectorAll('.group');
    
    interactiveElements.forEach(element => {
      element.addEventListener('mouseenter', function() {
        this.style.boxShadow = '0 0 30px rgba(6, 182, 212, 0.3)';
      });
      
      element.addEventListener('mouseleave', function() {
        this.style.boxShadow = 'none';
      });
    });
  });
</script>

<style>
  /* Custom scrollbar */
  :global(html) {
    scrollbar-width: thin;
    scrollbar-color: #0891b2 #1f2937;
  }
  
  :global(body::-webkit-scrollbar) {
    width: 8px;
  }
  
  :global(body::-webkit-scrollbar-track) {
    background: #1f2937;
  }
  
  :global(body::-webkit-scrollbar-thumb) {
    background: #0891b2;
    border-radius: 4px;
  }
  
  :global(body::-webkit-scrollbar-thumb:hover) {
    background: #0e7490;
  }

  /* Smooth transitions */
  * {
    transition-property: color, background-color, border-color, text-decoration-color, fill, stroke, opacity, box-shadow, transform, filter, backdrop-filter;
    transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
    transition-duration: 150ms;
  }

  /* Grid background pattern */
  main::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: 
      linear-gradient(rgba(6, 182, 212, 0.1) 1px, transparent 1px),
      linear-gradient(90deg, rgba(6, 182, 212, 0.1) 1px, transparent 1px);
    background-size: 50px 50px;
    pointer-events: none;
    z-index: 1;
  }

  /* Section spacing */
  section {
    margin-bottom: 4rem;
  }

  /* Animation delays for staggered loading */
  .group:nth-child(1) {
    animation-delay: 0.1s;
  }
  
  .group:nth-child(2) {
    animation-delay: 0.2s;
  }
  
  .group:nth-child(3) {
    animation-delay: 0.3s;
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    section {
      margin-bottom: 2rem;
    }
    
    .fixed {
      position: relative;
    }
  }
</style>
