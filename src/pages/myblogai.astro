---
import Layout from "../layouts/Layout.astro";
import Section from "../components/Section.astro";
import CosmeticText from "../components/CosmeticText.astro";

const title = "Mój Blog - Karol Lisson";
const description = "Osobisty blog Karola Lissona o technologii, AI, programowaniu i życiu.";

const cosmeticTexts: { [section: string]: string } = {
  header: "BLOG",
  hero: "PERSONAL",
  content: "STORIES",
  tech: "TECH"
};

// Blog posts data
const blogPosts = [
  {
    id: 1,
    title: "Moja podróż z AI i automatyzacją",
    excerpt: "Jak zacząłem tworzyć inteligentne systemy i dlaczego AI zmienia wszystko...",
    date: "2024-01-15",
    category: "AI & Tech",
    readTime: "5 min",
    featured: true
  },
  {
    id: 2,
    title: "Budowanie MyBonzo - od pomysłu do realizacji",
    excerpt: "Historia powstania platformy AI Workers i wyzwania techniczne...",
    date: "2024-01-10",
    category: "Projekty",
    readTime: "8 min",
    featured: true
  },
  {
    id: 3,
    title: "Backroom Aesthetic w web developmencie",
    excerpt: "Dlaczego wybrałem ciemny, industrialny design i jak wpływa na UX...",
    date: "2024-01-05",
    category: "Design",
    readTime: "4 min",
    featured: false
  },
  {
    id: 4,
    title: "Voice AI - przyszłość interfejsów",
    excerpt: "Implementacja systemów głosowych w aplikacjach webowych...",
    date: "2024-01-01",
    category: "AI & Tech",
    readTime: "6 min",
    featured: false
  }
];

const categories = ["Wszystkie", "AI & Tech", "Projekty", "Design", "Przemyślenia"];
---

<Layout title={title} description={description}>
  <main class="min-h-svh bg-primary text-primary-foreground font-primary">
    
    <!-- Hero Section with Backroom Style -->
    <Section class="relative flex min-h-72 pl-4 bg-radial-[at_50%_10%] from-accent/10 to-transparent">
      <div class="max-w-4xl mx-auto text-center my-auto px-8">
        <h1 class="text-6xl font-bold mb-6 text-accent">📝 Mój Blog</h1>
        <p class="text-xl mb-8 text-primary-foreground/80">
          Osobisty blog o technologii, AI, programowaniu i moich projektach.
        </p>
        <div class="text-sm text-primary-foreground/60">
          <span>Karol Lisson</span> • <span>AI Developer</span> • <span>Tech Enthusiast</span>
        </div>
      </div>
      <CosmeticText text={cosmeticTexts.hero} vertical />
    </Section>

    <!-- Categories Filter -->
    <Section class="py-8 border-b border-edge">
      <div class="max-w-6xl mx-auto px-8">
        <div class="flex flex-wrap justify-center gap-4">
          {categories.map(category => (
            <button 
              onclick={`filterByCategory('${category}')`}
              class="category-filter bg-secondary hover:bg-accent hover:text-primary text-primary-foreground px-6 py-2 rounded-interactive transition-all duration-interactive border border-edge"
              data-category={category}
            >
              {category}
            </button>
          ))}
        </div>
      </div>
    </Section>

    <!-- Featured Posts -->
    <Section class="py-16">
      <div class="max-w-6xl mx-auto px-8">
        <h2 class="text-3xl font-bold mb-8 text-accent flex items-center">
          ⭐ Polecane Artykuły
        </h2>
        
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-16">
          {blogPosts.filter(post => post.featured).map(post => (
            <article class="featured-post bg-secondary border border-edge rounded-interactive p-8 hover:border-accent transition-all duration-interactive group">
              <div class="flex items-center gap-4 mb-4 text-sm text-primary-foreground/60">
                <span class="bg-accent text-primary px-3 py-1 rounded text-xs font-semibold">{post.category}</span>
                <span>{post.date}</span>
                <span>• {post.readTime}</span>
              </div>
              
              <h3 class="text-2xl font-bold mb-4 text-primary-foreground group-hover:text-accent transition-colors">
                {post.title}
              </h3>
              
              <p class="text-primary-foreground/80 mb-6 leading-relaxed">
                {post.excerpt}
              </p>
              
              <button 
                onclick={`readPost(${post.id})`}
                class="inline-flex items-center text-accent hover:text-primary-foreground font-semibold transition-colors"
              >
                Czytaj więcej →
              </button>
            </article>
          ))}
        </div>
      </div>
      <CosmeticText text={cosmeticTexts.content} />
    </Section>

    <!-- All Posts -->
    <Section class="py-16 bg-secondary-30">
      <div class="max-w-6xl mx-auto px-8">
        <h2 class="text-3xl font-bold mb-8 text-accent flex items-center">
          📚 Wszystkie Artykuły
        </h2>
        
        <div class="space-y-6" id="posts-container">
          {blogPosts.map(post => (
            <article class="post-item bg-primary/50 border border-edge rounded-interactive p-6 hover:border-accent transition-all duration-interactive group" data-category={post.category}>
              <div class="flex flex-col lg:flex-row lg:items-center justify-between gap-4">
                <div class="flex-1">
                  <div class="flex items-center gap-4 mb-3 text-sm text-primary-foreground/60">
                    <span class="bg-accent/20 text-accent px-3 py-1 rounded text-xs font-semibold">{post.category}</span>
                    <span>{post.date}</span>
                    <span>• {post.readTime}</span>
                  </div>
                  
                  <h3 class="text-xl font-bold mb-3 text-primary-foreground group-hover:text-accent transition-colors">
                    {post.title}
                  </h3>
                  
                  <p class="text-primary-foreground/80 leading-relaxed">
                    {post.excerpt}
                  </p>
                </div>
                
                <div class="flex items-center gap-4">
                  <button 
                    onclick={`readPost(${post.id})`}
                    class="bg-accent hover:bg-accent/80 text-primary px-6 py-2 rounded-interactive font-semibold transition-all duration-interactive"
                  >
                    Czytaj
                  </button>
                </div>
              </div>
            </article>
          ))}
        </div>
      </div>
      <CosmeticText text={cosmeticTexts.tech} />
    </Section>

    <!-- About Section -->
    <Section class="py-16">
      <div class="max-w-4xl mx-auto px-8 text-center">
        <h2 class="text-3xl font-bold mb-8 text-accent">
          👨‍💻 O Mnie
        </h2>
        
        <div class="bg-secondary border border-edge rounded-interactive p-8">
          <p class="text-lg text-primary-foreground/80 mb-6 leading-relaxed">
            Cześć! Jestem Karol Lisson, programistą i pasjonatem AI. Tworzę inteligentne systemy, 
            automatyzuję procesy i eksperymentuję z nowymi technologiami. 
          </p>
          
          <p class="text-primary-foreground/80 mb-8 leading-relaxed">
            Na tym blogu dzielę się swoimi doświadczeniami z programowania, projektami AI, 
            przemyśleniami o technologii i wszystkim, co mnie inspiruje w świecie IT.
          </p>
          
          <div class="flex flex-wrap justify-center gap-4">
            <a href="/" class="bg-accent hover:bg-accent/80 text-primary px-6 py-2 rounded-interactive font-semibold transition-all duration-interactive">
              🏠 Strona Główna
            </a>
            <a href="mailto:kontakt@mybonzo.com" class="bg-secondary hover:bg-secondary/80 text-primary-foreground px-6 py-2 rounded-interactive font-semibold transition-all duration-interactive border border-edge">
              📧 Kontakt
            </a>
          </div>
        </div>
      </div>
    </Section>

  </main>
</Layout>

<script>
  // Filter posts by category
  function filterByCategory(category: string) {
    const posts = document.querySelectorAll('.post-item');
    const filters = document.querySelectorAll('.category-filter');
    
    // Update active filter
    filters.forEach(filter => {
      filter.classList.remove('bg-accent', 'text-primary');
      filter.classList.add('bg-secondary', 'text-primary-foreground');
    });
    
    const activeFilter = document.querySelector(`[data-category="${category}"]`);
    if (activeFilter) {
      activeFilter.classList.remove('bg-secondary', 'text-primary-foreground');
      activeFilter.classList.add('bg-accent', 'text-primary');
    }
    
    // Filter posts
    posts.forEach(post => {
      const postCategory = post.getAttribute('data-category');
      if (category === 'Wszystkie' || postCategory === category) {
        post.style.display = 'block';
      } else {
        post.style.display = 'none';
      }
    });
  }

  // Read post function
  function readPost(postId: number) {
    // In a real blog, this would navigate to the full post
    // For now, we'll show an alert
    const posts = {
      1: {
        title: "Moja podróż z AI i automatyzacją", 
        content: "Szczegółowy artykuł o tym, jak zaczął się mój kontakt z AI..."
      },
      2: {
        title: "Budowanie MyBonzo - od pomysłu do realizacji",
        content: "Historia powstania platformy AI Workers..."
      },
      3: {
        title: "Backroom Aesthetic w web developmencie",
        content: "Dlaczego wybrałem ciemny, industrialny design..."
      },
      4: {
        title: "Voice AI - przyszłość interfejsów",
        content: "Implementacja systemów głosowych..."
      }
    };
    
    const post = posts[postId as keyof typeof posts];
    if (post) {
      alert(`Czytasz: "${post.title}"\n\n${post.content}\n\n(W pełnej wersji bloga będzie to osobna strona)`);
    }
  }

  // Make functions global
  (window as any).filterByCategory = filterByCategory;
  (window as any).readPost = readPost;

  // Initialize - show all posts by default
  document.addEventListener('DOMContentLoaded', () => {
    filterByCategory('Wszystkie');
  });
</script>

<!-- Backroom Theme CSS -->
<style>
  /* Backroom Color Theme Variables */
  :root {
    --theme-hue: 200;
    --color-primary: hsl(var(--theme-hue), 20%, 8%);
    --color-primary-foreground: hsl(var(--theme-hue), 100%, 92%);
    --color-secondary: hsl(var(--theme-hue), 40%, 12%);
    --color-accent: hsl(var(--theme-hue), 100%, 50%);
    --color-edge: hsl(var(--theme-hue), 30%, 18%);
    --font-primary: Rajdhani, system-ui;
    --radius-interactive: 0.4rem;
    --duration-interactive: 200ms;
  }

  /* Apply dark/glassy styling for "white windows" */
  .featured-post, .post-item {
    background: rgba(0, 0, 0, 0.4) !important;
    backdrop-filter: blur(10px);
    border: 1px solid var(--color-edge);
  }

  .featured-post:hover, .post-item:hover {
    background: rgba(0, 0, 0, 0.6) !important;
    backdrop-filter: blur(15px);
    border-color: var(--color-accent);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.3);
  }

  /* Dark glassy containers */
  .bg-secondary {
    background: rgba(0, 0, 0, 0.3) !important;
    backdrop-filter: blur(8px);
  }

  .bg-secondary-30 {
    background: rgba(0, 0, 0, 0.2) !important;
  }

  /* Category filters styling */
  .category-filter {
    backdrop-filter: blur(5px);
    transition: all var(--duration-interactive);
  }

  .category-filter:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
  }

  /* Typography improvements */
  h1, h2, h3 {
    font-family: var(--font-primary);
    letter-spacing: 0.5px;
  }

  /* Interactive elements */
  button, a {
    transition: all var(--duration-interactive);
  }

  button:hover, a:hover {
    transform: translateY(-1px);
  }

  /* Responsive design */
  @media (max-width: 768px) {
    .featured-post, .post-item {
      margin: 0 10px;
    }
    
    .category-filter {
      font-size: 0.9rem;
      padding: 8px 16px;
    }
  }

  /* Smooth animations */
  .post-item {
    animation: fadeIn 0.5s ease-in-out;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Scrollbar styling */
  ::-webkit-scrollbar {
    width: 8px;
  }

  ::-webkit-scrollbar-track {
    background: var(--color-primary);
  }

  ::-webkit-scrollbar-thumb {
    background: var(--color-edge);
    border-radius: 4px;
  }

  ::-webkit-scrollbar-thumb:hover {
    background: var(--color-accent);
  }
</style>

    <!-- Features Grid with Backroom Style -->
    <Section class="grid md:grid-cols-3 gap-8">
      <div class="bg-secondary p-6 rounded-interactive border border-edge hover:bg-secondary/80 transition-all duration-interactive">
        <div class="text-3xl mb-4 text-accent">🤖</div>
        <h3 class="text-xl font-bold mb-3 text-primary-foreground">AI Content Generator</h3>
        <p class="text-primary-foreground/70">Automatyczne tworzenie wysokiej jakości treści blogowych z wykorzystaniem zaawansowanych modeli AI.</p>
      </div>
      
      <div class="bg-secondary p-6 rounded-interactive border border-edge hover:bg-secondary/80 transition-all duration-interactive">
        <div class="text-3xl mb-4 text-accent">🔍</div>
        <h3 class="text-xl font-bold mb-3 text-primary-foreground">SEO Optimization</h3>
        <p class="text-primary-foreground/70">Automatyczna optymalizacja SEO, słowa kluczowe, meta opisy i struktura artykułów.</p>
      </div>
      
      <div class="bg-secondary p-6 rounded-interactive border border-edge hover:bg-secondary/80 transition-all duration-interactive">
        <div class="text-3xl mb-4 text-accent">📊</div>
        <h3 class="text-xl font-bold mb-3 text-primary-foreground">Analytics & Insights</h3>
        <p class="text-primary-foreground/70">Szczegółowe analizy wydajności treści i rekomendacje dotyczące poprawy zasięgu.</p>
      </div>
    </Section>

    <!-- Blog Generator Interface with Backroom Style -->
    <Section id="blog-generator" class="relative bg-secondary rounded-interactive border border-edge p-8" style="display: none;">
      <span class="absolute"><CosmeticText text={cosmeticTexts.generator} vertical /></span>
      <h2 class="text-3xl font-bold mb-6 text-accent pl-16">✨ Generator Bloga AI</h2>
      
      <div class="grid md:grid-cols-2 gap-8">
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">Temat bloga:</label>
          <input 
            type="text" 
            id="blog-topic"
            placeholder="np. 'Najlepsze praktyki programowania w JavaScript'"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
          />
        </div>
        
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">Kategoria:</label>
          <select 
            id="blog-category"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
          >
            <option value="">Wybierz kategorię</option>
            <option value="technology">Technologia</option>
            <option value="programming">Programowanie</option>
            <option value="ai">Sztuczna Inteligencja</option>
            <option value="business">Biznes</option>
            <option value="lifestyle">Lifestyle</option>
            <option value="education">Edukacja</option>
          </select>
        </div>
        
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">Długość artykułu:</label>
          <select 
            id="blog-length"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
          >
            <option value="short">Krótki (500-800 słów)</option>
            <option value="medium">Średni (800-1500 słów)</option>
            <option value="long">Długi (1500+ słów)</option>
          </select>
        </div>
        
        <div>
          <label class="block text-sm font-medium text-gray-700 mb-2">Ton pisania:</label>
          <select 
            id="blog-tone"
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
          >
            <option value="professional">Profesjonalny</option>
            <option value="casual">Casualowy</option>
            <option value="academic">Akademicki</option>
            <option value="friendly">Przyjazny</option>
            <option value="technical">Techniczny</option>
          </select>
        </div>
      </div>
      
      <div class="mt-6">
        <label class="block text-sm font-medium text-gray-700 mb-2">Dodatkowe instrukcje (opcjonalne):</label>
        <textarea 
          id="blog-instructions"
          placeholder="Dodatkowe wymagania, słowa kluczowe, styl pisania..."
          rows="3"
          class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
        ></textarea>
      </div>
      
      <div class="flex gap-4 mt-8">
        <button 
          onclick="generateBlogPost()"
          class="bg-green-600 hover:bg-green-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors"
        >
          🎯 Generuj Artykuł
        </button>
        <button 
          onclick="saveDraft()"
          class="bg-gray-600 hover:bg-gray-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors"
        >
          💾 Zapisz Szkic
        </button>
      </div>
    </section>

    <!-- Blog List -->
    <section id="blog-list" class="bg-white rounded-lg shadow-lg p-8" style="display: none;">
      <h2 class="text-3xl font-bold mb-6">📚 Moje Blogi</h2>
      <div id="blogs-container" class="space-y-4">
        <p class="text-gray-500 text-center py-8">Nie masz jeszcze żadnych blogów. Rozpocznij pisanie!</p>
      </div>
    </section>

    <!-- Generated Content Preview -->
    <section id="content-preview" class="bg-white rounded-lg shadow-lg p-8 mt-8" style="display: none;">
      <h2 class="text-3xl font-bold mb-6">📄 Podgląd Artykułu</h2>
      <div id="preview-content" class="prose max-w-none">
        <!-- Generated content will appear here -->
      </div>
      <div class="flex gap-4 mt-8">
        <button 
          onclick="publishBlog()"
          class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors"
        >
          🚀 Publikuj
        </button>
        <button 
          onclick="editBlog()"
          class="bg-yellow-600 hover:bg-yellow-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors"
        >
          ✏️ Edytuj
        </button>
        <button 
          onclick="regenerateBlog()"
          class="bg-purple-600 hover:bg-purple-700 text-white px-6 py-3 rounded-lg font-semibold transition-colors"
        >
          🔄 Regeneruj
        </button>
      </div>
    </section>
  </main>

  <script>
    // Blog Generator Functions
    function startBlogGenerator() {
      document.getElementById('blog-generator').style.display = 'block';
      document.getElementById('blog-list').style.display = 'none';
      document.getElementById('content-preview').style.display = 'none';
    }

    function showBlogList() {
      document.getElementById('blog-list').style.display = 'block';
      document.getElementById('blog-generator').style.display = 'none';
      document.getElementById('content-preview').style.display = 'none';
      loadBlogList();
    }

    async function generateBlogPost() {
      const topic = document.getElementById('blog-topic').value;
      const category = document.getElementById('blog-category').value;
      const length = document.getElementById('blog-length').value;
      const tone = document.getElementById('blog-tone').value;
      const instructions = document.getElementById('blog-instructions').value;

      if (!topic.trim()) {
        alert('Proszę podać temat bloga!');
        return;
      }

      // Show loading state
      const generateBtn = event.target;
      const originalText = generateBtn.textContent;
      generateBtn.textContent = '⏳ Generuję...';
      generateBtn.disabled = true;

      try {
        const response = await fetch('/api/generate-blog', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
          },
          body: JSON.stringify({
            topic,
            category,
            length,
            tone,
            instructions
          })
        });

        const result = await response.json();
        
        if (result.success) {
          document.getElementById('preview-content').innerHTML = result.content;
          document.getElementById('content-preview').style.display = 'block';
          document.getElementById('blog-generator').scrollIntoView({ behavior: 'smooth' });
        } else {
          alert('Błąd podczas generowania: ' + result.error);
        }
      } catch (error) {
        console.error('Error generating blog:', error);
        alert('Wystąpił błąd podczas generowania bloga.');
      } finally {
        generateBtn.textContent = originalText;
        generateBtn.disabled = false;
      }
    }

    function saveDraft() {
      const blogData = {
        topic: document.getElementById('blog-topic').value,
        category: document.getElementById('blog-category').value,
        length: document.getElementById('blog-length').value,
        tone: document.getElementById('blog-tone').value,
        instructions: document.getElementById('blog-instructions').value,
        timestamp: new Date().toISOString()
      };

      // Save to localStorage for now
      const drafts = JSON.parse(localStorage.getItem('blog-drafts') || '[]');
      drafts.push(blogData);
      localStorage.setItem('blog-drafts', JSON.stringify(drafts));
      
      alert('Szkic został zapisany!');
    }

    function loadBlogList() {
      const drafts = JSON.parse(localStorage.getItem('blog-drafts') || '[]');
      const container = document.getElementById('blogs-container');
      
      if (drafts.length === 0) {
        container.innerHTML = '<p class="text-gray-500 text-center py-8">Nie masz jeszcze żadnych blogów. Rozpocznij pisanie!</p>';
        return;
      }

      container.innerHTML = drafts.map((draft, index) => `
        <div class="border rounded-lg p-4 hover:shadow-md transition-shadow">
          <h3 class="font-bold text-lg mb-2">${draft.topic}</h3>
          <div class="flex flex-wrap gap-2 mb-2">
            <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded text-sm">${draft.category}</span>
            <span class="bg-green-100 text-green-800 px-2 py-1 rounded text-sm">${draft.length}</span>
            <span class="bg-purple-100 text-purple-800 px-2 py-1 rounded text-sm">${draft.tone}</span>
          </div>
          <p class="text-gray-600 text-sm mb-3">Utworzono: ${new Date(draft.timestamp).toLocaleDateString('pl-PL')}</p>
          <div class="flex gap-2">
            <button onclick="editDraft(${index})" class="bg-blue-500 text-white px-3 py-1 rounded text-sm">Edytuj</button>
            <button onclick="deleteDraft(${index})" class="bg-red-500 text-white px-3 py-1 rounded text-sm">Usuń</button>
          </div>
        </div>
      `).join('');
    }

    function editDraft(index) {
      const drafts = JSON.parse(localStorage.getItem('blog-drafts') || '[]');
      const draft = drafts[index];
      
      document.getElementById('blog-topic').value = draft.topic;
      document.getElementById('blog-category').value = draft.category;
      document.getElementById('blog-length').value = draft.length;
      document.getElementById('blog-tone').value = draft.tone;
      document.getElementById('blog-instructions').value = draft.instructions || '';
      
      startBlogGenerator();
    }

    function deleteDraft(index) {
      if (confirm('Czy na pewno chcesz usunąć ten szkic?')) {
        const drafts = JSON.parse(localStorage.getItem('blog-drafts') || '[]');
        drafts.splice(index, 1);
        localStorage.setItem('blog-drafts', JSON.stringify(drafts));
        loadBlogList();
      }
    }

    function publishBlog() {
      alert('Funkcja publikacji będzie dostępna wkrótce!');
    }

    function editBlog() {
      alert('Funkcja edycji będzie dostępna wkrótce!');
    }

    function regenerateBlog() {
      generateBlogPost();
    }

    // Add to global scope for the main page button
    window.launchMyBlogAI = function() {
      startBlogGenerator();
    }
  </script>

  <style>
    .prose {
      max-width: none;
    }
    
    .prose h1, .prose h2, .prose h3 {
      color: #1f2937;
      font-weight: 700;
    }
    
    .prose p {
      margin-bottom: 1rem;
      line-height: 1.7;
    }
    
    .prose ul, .prose ol {
      margin: 1rem 0;
      padding-left: 2rem;
    }
    
    .prose li {
      margin-bottom: 0.5rem;
    }
  </style>
</Layout>
