---
// Dashboard agentów - rozszerzone funkcje
import Layout from '@/layouts/Layout.astro';

// Lista dostępnych agentów z folderu dodatki
const availableAgents = [
  {
    id: 'voice',
    name: 'Voice Command Agent',
    description: 'Rozpoznawanie głosu i przetwarzanie komend w języku polskim',
    status: 'active',
    capabilities: ['voice_recognition', 'polish_language', 'command_processing'],
    color: '#ff6b9d'
  },
  {
    id: 'music',
    name: 'Music Control Agent', 
    description: 'Kontrola odtwarzania muzyki i zarządzanie playlistami',
    status: 'active',
    capabilities: ['music_control', 'playlist_management', 'audio_streaming'],
    color: '#9333ea'
  },
  {
    id: 'analytics',
    name: 'Analytics Prophet Agent',
    description: 'Zaawansowana analiza danych i predykcje biznesowe',
    status: 'development',
    capabilities: ['data_analysis', 'predictions', 'business_intelligence'],
    color: '#ef4444'
  },
  {
    id: 'business',
    name: 'Business Assistant Agent',
    description: 'Wsparcie procesów biznesowych i automatyzacja zadań',
    status: 'active',
    capabilities: ['business_automation', 'task_management', 'workflow'],
    color: '#ffc107'
  },
  {
    id: 'security',
    name: 'Security Guard Agent',
    description: 'Monitoring bezpieczeństwa i detekcja zagrożeń',
    status: 'active',
    capabilities: ['security_monitoring', 'threat_detection', 'auth_management'],
    color: '#00ff88'
  },
  {
    id: 'webmaster',
    name: 'Webmaster Agent',
    description: 'Zarządzanie stronami web i optymalizacja SEO',
    status: 'development',
    capabilities: ['website_management', 'seo_optimization', 'performance_monitoring'],
    color: '#1be1ff'
  }
];
---

<Layout title="Agenci MyBonzo - Inteligentne Moduły">
  <main class="min-h-svh relative z-10">
    <!-- Background -->
    <div class="fixed inset-0 bg-[#0a0f14]">
      <div class="absolute inset-0 bg-gradient-to-br from-cyan-900/20 via-[#0a0f14] to-[#0a0f14]"></div>
    </div>

    <!-- Header -->
    <section class="relative z-20 border-b border-[#263238] pt-20">
      <div class="max-w-6xl mx-auto px-4 py-8">
        <div class="flex items-center justify-between mb-8">
          <div>
            <h1 class="text-4xl md:text-5xl font-bold text-[#1be1ff] mb-2 uppercase tracking-wider font-['Rajdhani']">
              🤖 AGENCI AI
            </h1>
            <p class="text-[#e1f5fe] text-lg">
              System inteligentnych agentów z rozszerzonymi funkcjami
            </p>
          </div>
          <div class="text-right text-sm text-[#b0bec5] font-mono">
            <div class="mb-1">
              AKTYWNE: <span class="text-[#00ff88]">{availableAgents.filter(a => a.status === 'active').length}</span>
            </div>
            <div>
              ROZWÓJ: <span class="text-[#ffc107]">{availableAgents.filter(a => a.status === 'development').length}</span>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Agents Grid -->
    <section class="relative z-20 py-12">
      <div class="max-w-6xl mx-auto px-4">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          
          {availableAgents.map(agent => (
            <div class="bg-[#0f3846]/60 border border-[#1be1ff]/20 rounded-lg p-6 hover:bg-[#0f3846]/80 transition-all duration-300">
              <!-- Agent Header -->
              <div class="flex items-center justify-between mb-4">
                <div class="flex items-center">
                  <div class={`w-10 h-10 rounded-lg flex items-center justify-center mr-3`} 
                       style={`background: ${agent.color}20; border: 1px solid ${agent.color}50;`}>
                    🤖
                  </div>
                  <div>
                    <h3 class="text-lg font-bold font-['Rajdhani']" style={`color: ${agent.color};`}>
                      {agent.name.toUpperCase()}
                    </h3>
                  </div>
                </div>
                <div class={`px-2 py-1 rounded text-xs font-mono ${
                  agent.status === 'active' 
                    ? 'bg-[#00ff88]/20 text-[#00ff88] border border-[#00ff88]/30'
                    : 'bg-[#ffc107]/20 text-[#ffc107] border border-[#ffc107]/30'
                }`}>
                  {agent.status.toUpperCase()}
                </div>
              </div>

              <!-- Description -->
              <p class="text-[#e1f5fe] mb-4 text-sm leading-relaxed">
                {agent.description}
              </p>

              <!-- Capabilities -->
              <div class="mb-4">
                <div class="text-xs text-[#b0bec5] mb-2 font-mono">MOŻLIWOŚCI:</div>
                <div class="flex flex-wrap gap-1">
                  {agent.capabilities.map(cap => (
                    <span class="px-2 py-1 bg-[#0f3846]/80 border border-[#1be1ff]/20 rounded text-xs text-[#1be1ff] font-mono">
                      {cap}
                    </span>
                  ))}
                </div>
              </div>

              <!-- Actions -->
              <div class="flex gap-2">
                <button 
                  onclick={`window.location.href='/dodatki/agents/${agent.id}'`}
                  class="flex-1 bg-gradient-to-r from-[#1be1ff]/20 to-[#00e5ff]/20 border border-[#1be1ff]/30 text-[#1be1ff] px-3 py-2 rounded text-sm font-medium hover:bg-[#1be1ff]/10 transition-all duration-300"
                >
                  Uruchom
                </button>
                <button 
                  onclick={`console.log('Konfiguracja agenta: ${agent.id}')`}
                  class="px-3 py-2 bg-[#0f3846]/60 border border-[#263238] text-[#b0bec5] rounded text-sm hover:text-[#e1f5fe] hover:border-[#1be1ff]/20 transition-all duration-300"
                >
                  ⚙️
                </button>
              </div>
            </div>
          ))}

        </div>
      </div>
    </section>

    <!-- Navigation -->
    <section class="relative z-20 py-8 border-t border-[#263238]">
      <div class="max-w-6xl mx-auto px-4">
        <div class="flex justify-between items-center">
          <a href="/dodatki/" class="inline-flex items-center text-[#1be1ff] hover:text-[#00e5ff] font-medium">
            ← Powrót do Dodatków
          </a>
          <div class="text-sm text-[#b0bec5] font-mono">
            Łącznie agentów: {availableAgents.length}
          </div>
        </div>
      </div>
    </section>

  </main>
</Layout>
